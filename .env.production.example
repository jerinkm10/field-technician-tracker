POSTGRES_DB=field_technician_tracker
POSTGRES_USER=fieldtech
POSTGRES_PASSWORD=change-this-db-password
DATABASE_URL=postgresql://fieldtech:change-this-db-password@postgres:5432/field_technician_tracker?schema=public

JWT_SECRET=change-this-jwt-secret
JWT_EXPIRES_IN=1d

# Set this to your EC2 public URL or domain. Use commas for multiple origins.
CORS_ORIGIN=http://13.126.89.105

# Leave these defaults when the Angular app and API share the same host.
APP_API_BASE_URL=/api
APP_SOCKET_URL=
APP_GOOGLE_MAPS_API_KEY=

# Change only if port 80 is already occupied on the server.
WEB_PORT=80
